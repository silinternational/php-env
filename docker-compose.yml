version: '2'
services:
    test:
        image: silintl/php7:latest
        volumes:
            - ./:/data
        working_dir: /data
        env_file:
            - ./local.env
        command: ["true"]

